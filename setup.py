from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A machine learning project that predicts estimated food delivery time based on real-world factors such as restaurant location, delivery distance, order volume, traffic conditions, and more.',
    author='Aryan',
    license='',
)
